program: main.py
name: soft-prompts-ner
method: grid
parameters:
  dataset:
    values: [turkish-ner]
  train.num_epochs:
    values: [5, 10, 25, 50]
  basic.task:
    values: [NER]
  basic.lang:
    values: [TR]
  basic.with_soft_prompts:
    values: [True]
  soft_prompts.num_virtual_tokens:
    values: [1, 5, 10, 20, 50]
  soft_prompts.evaluate:
    values: [False]
command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}